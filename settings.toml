

[default]

# === Project Identity (from onboarding and MRM) ===
MODEL_ID = "HACKATHON_UNIBG"
MODEL_VERSION = "1.0.0"
USECASE = "aitxy"

# === GCP Infrastructure ===
REGION = "europe-west4"

# === ETL Stage Paths ===
EXTRACT_INPUT_PATH = "raw"
EXTRACT_OUTPUT_PATH = "extract"
PREPROC_INPUT_PATH = "extract"
PREPROC_OUTPUT_PATH = "preproc"
PRED_INPUT_PATH = "preproc"
PRED_OUTPUT_PATH = "pred"
POSTPROC_INPUT_PATH = "pred"
POSTPROC_OUTPUT_PATH = "postproc"
EXPORT_INPUT_PATH = "postproc"
EXPORT_OUTPUT_PATH = "export"


# === Docker ===
TAG = "latest"
DOCKER_IMAGE_BASE = "base"
DOCKER_IMAGE_MONITOR = "monitor"
[default.VERTEX_JOB_ARGS]
replica_count = 1
machine_type = "e2-standard-4"
strategy = "STANDARD"
